# Plugin Usage

This example contains a minimal plugin manifest and a small runner that validates the manifest shape.

```sh
node examples/plugin-usage/run-plugin.js examples/plugin-usage/plugin.json
```

The manifest shows how a plugin can declare commands and wire an MCP server command.
