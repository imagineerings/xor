# Frontend Tools

This static example demonstrates a browser-side tool client without any build step.

Open `index.html` in a browser and click the button to invoke the local `summarize_text` tool implemented in `tool-client.js`.
