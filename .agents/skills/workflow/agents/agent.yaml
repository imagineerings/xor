interface:
  display_name: "Workflow"
  short_description: "Begin the next unclaimed local task."
  default_prompt: "Run the workflow skill to automatically begin the next unclaimed .agents/specs task, create its Linear issue without duplicating existing work, render WORKFLOW.md, and work from the returned prompt."

policy:
  allow_implicit_invocation: true
