{
  "schema_version": 1,
  "resolution_module": "elementwise_or_runtime_operation_03",
  "operation_id": "COMFY-TENSOR-OP-26EF6B18C684",
  "baseline_overload_id": "COMFY-TENSOR-OP-26EF6B18C684:blocked",
  "baseline_fixture_sha256": "c3c8230dba45ed0e82188feb52a13cbd2fadfaec72708630b2876ef1df033b6e",
  "overload_id": "zed.native.tensor.clamp-in-place.cpu.v1",
  "owner_task_id": "comfy-parity-tensor-ops-elementwise-or-runtime-operation-comfy-tensor-op-1917b7227a5c",
  "ordered_parameters": [
    {
      "default": null,
      "keyword_only": false,
      "kind": "positional_or_keyword",
      "name": "input",
      "type": "ordered Tensor"
    },
    {
      "default": "None",
      "keyword_only": true,
      "kind": "keyword_only",
      "name": "min",
      "type": "optional Scalar"
    },
    {
      "default": "None",
      "keyword_only": true,
      "kind": "keyword_only",
      "name": "max",
      "type": "optional Scalar"
    }
  ],
  "output_types": [
    "()"
  ],
  "semantics": {
    "rust_signature": "fn clamp_in_place_with_context_exact_native(backend: &CpuBackend, input: &mut Tensor, minimum: Option<Scalar>, maximum: Option<Scalar>, context: &ExecutionContext<'_>) -> Result<(), ElementwiseRuntimePartThreeError>",
    "mutation_rule": "transactional in place; all values are staged before one copy-on-write publication",
    "alias_rule": "preserves the receiver handle and uses canonical copy-on-write",
    "shape_rule": "shape is unchanged including scalar and empty tensors",
    "dtype_rule": "real and integer receiver dtype is preserved; complex and boolean fail typed",
    "accumulation_dtype": "none",
    "layout_rule": "logical values use canonical strides and TensorWrite",
    "device_rule": "CPU ordinal zero only",
    "numeric_rule": "inclusive clamp with IEEE NaN propagation; when minimum exceeds maximum every finite value becomes maximum",
    "tolerance": "exact canonical encoding",
    "determinism": "fixed row-major staging",
    "cancellation_points": "before validation, at bounded staging intervals, before write, and before publication",
    "vjp_rule": "mutation is journaled by the canonical autograd owner; no standalone VJP entry point",
    "jvp_rule": "same as VJP reachability"
  }
}
