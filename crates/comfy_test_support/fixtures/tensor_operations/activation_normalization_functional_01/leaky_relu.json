{
  "schema_version": 1,
  "resolution_module": "activation_normalization_functional_01",
  "operation_id": "COMFY-TENSOR-OP-8F7DF3AF61B9",
  "baseline_overload_id": "COMFY-TENSOR-OP-8F7DF3AF61B9:blocked",
  "baseline_fixture_sha256": "03a8d258b0f4d24854256136193f5f7409e7d15547ba2aca6ff38bab01b55e03",
  "overload_id": "sim.native.functional.leaky_relu.cpu_f32.v1",
  "owner_task_id": "comfy-parity-tensor-ops-activation-normalization-functional-comfy-tensor-op-0538873a73b1",
  "ordered_parameters": [
    {"default": null, "keyword_only": false, "kind": "positional_or_keyword", "name": "input", "type": "contiguous f32 tensor slice"},
    {"default": 0.01, "keyword_only": false, "kind": "positional_or_keyword", "name": "negative_slope", "type": "f32"},
    {"default": false, "keyword_only": true, "kind": "keyword_only", "name": "in_place", "type": "bool"}
  ],
  "output_types": ["Vec<f32> or in-place unit"],
  "semantics": {
    "rust_signature": "fn leaky_relu_with_context_exact_native(backend: &CpuBackend, input: &[f32], negative_slope: f32, device: DeviceId, context: &ExecutionContext<'_>) -> Result<Vec<f32>, FunctionalError>; fn leaky_relu_with_context_exact_native_in_place(backend: &CpuBackend, input: &mut [f32], negative_slope: f32, device: DeviceId, context: &ExecutionContext<'_>) -> Result<(), FunctionalError>",
    "mutation_rule": "out-of-place entry leaves input immutable; explicit in-place entry atomically replaces the same slice only after complete success",
    "alias_rule": "out-of-place returns fresh storage; in-place preserves the caller-provided allocation identity",
    "shape_rule": "preserves every input element and logical shape",
    "dtype_rule": "the exact native slice accepts contiguous f32 values and a finite f32 negative slope",
    "accumulation_dtype": "f32 elementwise arithmetic",
    "layout_rule": "layout-independent elementwise contiguous logical order",
    "device_rule": "CPU exact native execution; non-CPU requires a separately certified adapter and returns typed unavailable here",
    "numeric_rule": "nonnegative values are unchanged and negative values are multiplied by negative_slope with NaN propagation",
    "tolerance": "exact f32 branch arithmetic and absolute 0.0003 for finite-difference gradient fixtures",
    "determinism": "fixed scalar formula with no RNG",
    "cancellation_points": "before temporary allocation, at bounded element intervals, and before in-place commit; cancellation leaves input unchanged",
    "vjp_rule": "analytical piecewise VJP using slope one for nonnegative inputs and negative_slope for negative inputs",
    "jvp_rule": "analytical piecewise elementwise JVP with the same zero convention"
  }
}
