{
  "schema_version": 1,
  "resolution_module": "activation_normalization_functional_01",
  "operation_id": "COMFY-TENSOR-OP-0538873A73B1",
  "baseline_overload_id": "COMFY-TENSOR-OP-0538873A73B1:blocked",
  "baseline_fixture_sha256": "bf03730f2c5ab08780538044de0bfca378e66538332325acf9e8cccf0a8df2c5",
  "overload_id": "sim.native.functional.relu.cpu_f32.v1",
  "owner_task_id": "comfy-parity-tensor-ops-activation-normalization-functional-comfy-tensor-op-0538873a73b1",
  "ordered_parameters": [
    {"default": null, "keyword_only": false, "kind": "positional_or_keyword", "name": "input", "type": "contiguous f32 tensor slice"},
    {"default": false, "keyword_only": true, "kind": "keyword_only", "name": "in_place", "type": "bool"}
  ],
  "output_types": ["Vec<f32> or in-place unit"],
  "semantics": {
    "rust_signature": "fn relu_with_context_exact_native(backend: &CpuBackend, input: &[f32], device: DeviceId, context: &ExecutionContext<'_>) -> Result<Vec<f32>, FunctionalError>; fn relu_with_context_exact_native_in_place(backend: &CpuBackend, input: &mut [f32], device: DeviceId, context: &ExecutionContext<'_>) -> Result<(), FunctionalError>",
    "mutation_rule": "out-of-place entry leaves input immutable; explicit in-place entry atomically replaces the same slice only after complete success",
    "alias_rule": "out-of-place returns fresh storage; in-place preserves the caller-provided allocation identity",
    "shape_rule": "preserves every input element and logical shape",
    "dtype_rule": "the functional adapter accepts contiguous f32 and delegates scalar ReLU values to CpuBackend's canonical unary implementation",
    "accumulation_dtype": "f32 elementwise scalar dispatch",
    "layout_rule": "layout-independent elementwise contiguous logical order",
    "device_rule": "CPU exact native execution; non-CPU requires a separately certified adapter and returns typed unavailable here",
    "numeric_rule": "delegates canonical scalar maximum behavior including NaN propagation and uses derivative zero at zero",
    "tolerance": "exact scalar output and analytical gradient fixtures",
    "determinism": "fixed scalar dispatch with no RNG",
    "cancellation_points": "before temporary allocation, at bounded element intervals, and before in-place commit; cancellation leaves input unchanged",
    "vjp_rule": "analytical piecewise VJP with derivative one for positive values and zero otherwise",
    "jvp_rule": "analytical piecewise elementwise JVP with derivative zero at zero"
  }
}
