{
  "schema_version": 1,
  "resolution_module": "comfy_operator_indirection_02",
  "operation_id": "COMFY-TENSOR-OP-DAC4074BC3B2",
  "baseline_overload_id": "COMFY-TENSOR-OP-DAC4074BC3B2:blocked",
  "baseline_fixture_sha256": "94c25f2f0be573081add1b73896ec74a262f43c18c2b2a99000c4d20a85fe0c5",
  "overload_id": "sim.native.model.manual_cast.linear.v1",
  "owner_task_id": "comfy-parity-tensor-ops-comfy-operator-indirection-comfy-tensor-op-a0bd98dda517",
  "ordered_parameters": [
    {"default": null, "keyword_only": false, "kind": "positional_or_keyword", "name": "input_features", "type": "nonzero usize"},
    {"default": null, "keyword_only": false, "kind": "positional_or_keyword", "name": "output_features", "type": "nonzero usize"},
    {"default": true, "keyword_only": false, "kind": "positional_or_keyword", "name": "bias", "type": "bool"}
  ],
  "output_types": ["NativeModule"],
  "semantics": {
    "rust_signature": "fn manual_cast_linear_exact_native(layer_name: impl Into<String>, input_features: usize, output_features: usize, bias: bool) -> Result<NativeModule, NativeOpsError>",
    "mutation_rule": "constructor creates unloaded checked module state and forward casts without mutating stored parameters",
    "alias_rule": "canonical cast alias rules apply to immutable parameters and output is fresh",
    "shape_rule": "input trailing width, [output,input] weight, and optional [output] bias are checked",
    "dtype_rule": "weight and bias cast to the input-selected compute dtype through the canonical cast owner",
    "accumulation_dtype": "fixed f32 linear multiply-add order",
    "layout_rule": "contiguous logical rows with trailing feature width",
    "device_rule": "CPU exact native; uncertified devices return typed unavailable",
    "numeric_rule": "delegates the complete equation to the canonical linear kernel",
    "tolerance": "absolute 0.00001 forward and 0.003 finite-difference gradient",
    "determinism": "fixed row, output-feature, and input-feature order",
    "cancellation_points": "before casts, at bounded output features, and before output publication",
    "vjp_rule": "delegates analytical input, weight, and optional bias VJP to the linear owner",
    "jvp_rule": "delegates analytical input, weight, and optional bias JVP to the linear owner"
  }
}
