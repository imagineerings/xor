[project]
name = "baymax-eval"
version = "0.1.0"
description = "Harbor agent wrapper for Baymax's eval-cli"
requires-python = ">=3.12"
dependencies = ["harbor==0.13.0"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
